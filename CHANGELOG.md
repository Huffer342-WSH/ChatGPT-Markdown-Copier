# Changelog

## 0.4.0 — 2026-09-13

### 新增

- 增加扩展弹窗和设置页，可切换“点击行内代码选中内容”和“代码选区仅复制原文”，设置保存在本机并同步到已打开的页面。
- 支持点击行内代码直接选中内容，方便继续复制。
- 增加真实 ChatGPT 分享页的 Playwright 测试，覆盖两个回复复制按钮及 30 个选区场景，读取实际剪贴板并核对 Markdown、MathML、表格和选区边界。
- 浏览器兼容性检查合并到 CT，每周日北京时间 10:23 自动运行，也可手动触发；失败保留复制结果、截图和 trace。

### 修复

- 默认在同一代码块或行内代码内选取复制时只输出选中的代码，保留缩进，不额外添加 Markdown 围栏或反引号；跨越代码边界时仍保留 Markdown 格式。
- 完整选中嵌套列表单项时，只保留直属列表及原始编号，不再产生未选中的外层空编号。

### 开发与验证

- 扩展入口、核心代码及静态资源统一迁入 `src/`，更新架构与开发说明。
- Windows 和 Ubuntu 24.04 容器中的构建、类型检查、64 项单元测试及真实浏览器复制测试均已通过；保留项目专属 WSLC 容器用于后续验证。

## 0.3.1 — 2026-09-12

### 新增

- 回复操作区提供独立的原样复制和 Markdown 复制按钮，复用官方复制结果，修正代码区域外的行内公式分隔符。
- 选区复制保留标题、强调、链接、列表、引用、代码和表格格式；同一列表项内选中文字时不附带序号，跨项选择保留编号与层级。
- 表格支持单元格文本、无表头数据行和带表头表格三种复制方式；同时提供 Markdown 文本与带边框、表头样式的 HTML。
- 富文本公式转换为独立 MathML，支持粘贴到 Word；无法转换时保留可见 LaTeX，Markdown 输出保持不变。
- 测试集中到 `tests/`，增加真实 DOM 样本、选区和剪贴板回归用例，支持监听重跑及 Windows/Linux CI。
- 增加 `pnpm build:edge` 和 `pnpm build:edge:dev`，明确各构建命令的输出目录。
- README 增加 Edge 商店链接、功能演示视频，并更新功能说明、复制原理与结果截图；更新中英文扩展描述。

### 修复

- 转义构建产物中的 Unicode 非字符，修复 Edge 加载内容脚本时提示“不是 UTF-8 编码格式”的问题。
- 原样复制不再依赖 Markdown 桥接；失败时保留按钮，避免复制超时后按钮消失。
- 修复有序列表编号、子列表缩进、局部选区起始编号，以及列表内段落顺序和代码块格式丢失。
- 单元格局部选区不再被转换为表头；只选数据行时不补造表头分隔线。
- 修复代码中反引号破坏分隔符、行内公式错误跨越嵌套分隔符配对，以及 KaTeX HTML 粘贴到 Word 后公式丢失的问题。

## 0.3.0 — 2026-09-12

### 新增

- 支持选中回复中的公式并复制为 LaTeX；部分选中时扩展到完整公式，行内使用 `$...$`，块级使用 `$$...$$`。感谢 [@Kemoralocy](https://github.com/Kemoralocy) 的 [PR #1](https://github.com/Huffer342-WSH/ChatGPT-Markdown-Copier/pull/1)。
- 增加公式 DOM 和选区复制回归测试。
- 增加 `pnpm dev:edge` 命令与日常 Edge 浏览器调试说明。
- 正式版本发布后自动上传 Edge Add-ons 程序包并提交审核；预发布版本仅发布到 GitHub。

### 修复

- 兼容新版 ChatGPT 的 `data-math-source` 公式源属性，保留旧版 KaTeX annotation 兼容能力。
- 避免将包含公式的普通正文容器整体识别为块级公式，导致正文丢失。
- 移除项目直接声明的 Windows 专用构建依赖，由打包工具自动选择对应平台组件。

### 说明

- 普通文字和可编辑区域保留原生复制行为；公式源码不完整时回退原生复制。
- Edge 自动提审成功不代表已上架，最终上架仍需微软审核。
